let name1 = 'John';
let name2 = 'Alexis';
function reversName(name1, name2) {
  let friend1Length = name1.length;
  let friend2Length = name2.length;
  let largeName = '';
  if (friend1Length > friend2Length) {
    largeName = name1;
  } else{
    largeName = name2;
  }
  for (let i = 0; i < array.length; i++) {
    const element = array[i];
    
  }
  return largeName;
}
let result = reversName('John', 'Alexis');
console.log(result);